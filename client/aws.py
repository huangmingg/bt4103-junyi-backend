from xgboost import XGBClassifier
import os


def predict(x_train):
    # loading the model stored locally instead
    model_dir = f"./xgboost.model"
    # prediction = predict(model_dir, features)[0]
    pass
